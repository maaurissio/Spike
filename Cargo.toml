[package]
name = "spike"
version = "0.1.0"
edition = "2024"

[dependencies]
base64 = "0.22"
moka = { version = "0.12", default-features = false, features = ["sync"] }
native-tls = "0.2"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "json", "rustls-tls"] }
ratatui = "0.30.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tungstenite = "0.30"
crossterm = "0.29"

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.61", features = ["Win32_System_Console", "Win32_System_ProcessStatus", "Win32_System_Threading", "Win32_UI_Shell", "Win32_UI_WindowsAndMessaging"] }
